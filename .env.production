# Production Environment Variables for Vercel Deployment
# These variables should be set in Vercel Dashboard

# Frontend Build-time Variables (VITE_ prefix required)
VITE_API_URL=/api
VITE_APP_NAME=Order Management System
VITE_APP_VERSION=1.0.0

# Backend Runtime Variables (set in Vercel Dashboard)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/order-management?retryWrites=true&w=majority
# NODE_ENV=production
# PRODUCTION_URL=https://your-app.vercel.app

# Optional Vercel Storage Variables (auto-configured)
# KV_REST_API_URL=auto-configured
# KV_REST_API_TOKEN=auto-configured  
# BLOB_READ_WRITE_TOKEN=auto-configured
